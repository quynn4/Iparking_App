enum AuthenticationStatus {
  unknown,
  authenticated,
  unauthenticated,
  errAuthenticated,
}

enum StateBlocAppStatus { unknown, failure, success }

enum ResourceType { video, image, text, unknown }

enum TakePictureType { plate, vehicle, person }